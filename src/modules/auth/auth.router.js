// import Router from 'express';
// import { asyncHandler } from '../../middleware/asyncHandler.js';
// import { preSignUp, signIn, signUp, verifyAccount } from './auth.controller.js';
// import { signInVal, signVal } from './auth.validation.js';
// import { validation } from '../../middleware/validation.js';
// import { preSignupLimiter } from '../../middleware/rateLimiter.js';

// export const authRouter = Router();

// // pre-sign up
// authRouter.post('/pre-signup',preSignupLimiter,asyncHandler(preSignUp));
 

// authRouter.post('/sign-up',validation(signVal),signUp)

// authRouter.get('/verify-account',asyncHandler(verifyAccount))

// authRouter.post('/sign-in',validation(signInVal),signIn)




